# syntax=docker/dockerfile:1.7

FROM node:20-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci

EXPOSE 3000
CMD ["sh", "-c", "npm run dev -- --turbo -H 0.0.0.0 -p 3000 & if [ \"${WARMUP_ROUTES_ON_START:-1}\" = \"1\" ]; then node scripts/warmup-routes.mjs; fi; wait -n"]
