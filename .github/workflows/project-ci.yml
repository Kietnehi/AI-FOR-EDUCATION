name: CI Dự Án

on:
  push:
    paths:
      - "frontend/**"
      - "backend/**"
      - "docker-compose.yml"
      - "docker-compose.ci.yml"
      - ".env.docker.example"
      - ".github/workflows/project-ci.yml"
  pull_request:
    paths:
      - "frontend/**"
      - "backend/**"
      - "docker-compose.yml"
      - "docker-compose.ci.yml"
      - ".env.docker.example"
      - ".github/workflows/project-ci.yml"

permissions:
  contents: read
  issues: write

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    outputs:
      coverage: ${{ steps.extract-metrics.outputs.coverage }}
      coverage_details: ${{ steps.extract-metrics.outputs.coverage_details }}
      test_metrics: ${{ steps.extract-metrics.outputs.test_metrics }}
      ci_logs: ${{ steps.capture-logs.outputs.ci_logs }}

    steps:
      - name: Tải mã nguồn
        uses: actions/checkout@v6

      - name: Cài đặt Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Cài đặt dependencies
        run: npm ci 2>&1 | tee npm_ci.log

      - name: Chạy lint
        run: npm run lint

      - name: Chạy kiểm thử với coverage và xuất báo cáo JSON
        run: npx vitest run --coverage --reporter=default --reporter=json --outputFile=test-results.json 2>&1 | tee vitest.log

      - name: Trích xuất chỉ số (Metrics)
        id: extract-metrics
        if: always()
        run: |
          if [ -f coverage/coverage-summary.json ]; then
            TOTAL_LINES=$(jq -r '.total.lines.pct' coverage/coverage-summary.json)
            TOTAL_STMT=$(jq -r '.total.statements.pct' coverage/coverage-summary.json)
            TOTAL_FUNC=$(jq -r '.total.functions.pct' coverage/coverage-summary.json)
            TOTAL_BRANCH=$(jq -r '.total.branches.pct' coverage/coverage-summary.json)
            echo "coverage=$TOTAL_LINES" >> "$GITHUB_OUTPUT"
            echo "coverage_details={\"lines\":$TOTAL_LINES,\"statements\":$TOTAL_STMT,\"functions\":$TOTAL_FUNC,\"branches\":$TOTAL_BRANCH}" >> "$GITHUB_OUTPUT"
          else
            echo "coverage=0" >> "$GITHUB_OUTPUT"
            echo "coverage_details={\"lines\":0,\"statements\":0,\"functions\":0,\"branches\":0}" >> "$GITHUB_OUTPUT"
          fi

          if [ -f test-results.json ]; then
            PASSED=$(jq '.numPassedTests' test-results.json)
            FAILED=$(jq '.numFailedTests' test-results.json)
            TOTAL=$(jq '.numTotalTests' test-results.json)
            SKIPPED=$(jq '.numPendingTests' test-results.json)
            echo "test_metrics={\"passed\":$PASSED,\"failed\":$FAILED,\"total\":$TOTAL,\"skipped\":$SKIPPED}" >> "$GITHUB_OUTPUT"
          else
            echo "test_metrics={\"passed\":0,\"failed\":0,\"total\":0,\"skipped\":0}" >> "$GITHUB_OUTPUT"
          fi

      - name: Thu thập Logs Frontend
        id: capture-logs
        if: always()
        run: |
          echo "CI_LOGS_FE<<EOF" >> "$GITHUB_OUTPUT"
          echo "### 📦 npm ci output" >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          tail -n 20 npm_ci.log || echo "No logs" >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          echo "" >> "$GITHUB_OUTPUT"
          echo "### 🧪 vitest output" >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          tail -n 50 vitest.log || echo "No logs" >> "$GITHUB_OUTPUT"
          echo '```' >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"
          # Encode for safe passing
          LOGS_VAL=$(tail -n 100 vitest.log | base64 -w 0)
          echo "ci_logs=$LOGS_VAL" >> "$GITHUB_OUTPUT"

      - name: Tải artifact coverage frontend lên
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: frontend-coverage-report
          path: frontend/coverage
          if-no-files-found: ignore

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    outputs:
      coverage: ${{ steps.extract-metrics.outputs.coverage }}
      test_metrics: ${{ steps.extract-metrics.outputs.test_metrics }}
      ci_logs: ${{ steps.capture-logs.outputs.ci_logs }}

    steps:
      - name: Tải mã nguồn
        uses: actions/checkout@v6

      - name: Cài đặt Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: backend/requirements-test.txt

      - name: Cài đặt dependencies kiểm thử backend
        run: pip install -r requirements-test.txt 2>&1 | tee pip_install.log

      - name: Chạy kiểm thử backend với coverage và JUnit XML
        run: pytest --junitxml=junit.xml 2>&1 | tee pytest.log

      - name: Trích xuất chỉ số (Metrics)
        id: extract-metrics
        if: always()
        run: |
          # Trích xuất độ phủ (Coverage)
          if [ -f coverage.xml ]; then
            COVERAGE=$(python -c "import xml.etree.ElementTree as ET; tree = ET.parse('coverage.xml'); root = tree.getroot(); print(round(float(root.attrib['line-rate']) * 100, 2))")
            echo "coverage=$COVERAGE" >> "$GITHUB_OUTPUT"
          else
            echo "coverage=0" >> "$GITHUB_OUTPUT"
          fi

          # Trích xuất kết quả kiểm thử (Test Results)
          if [ -f junit.xml ]; then
            python -c "import xml.etree.ElementTree as ET, json; \
              tree = ET.parse('junit.xml'); \
              root = tree.getroot(); \
              total = int(root.attrib.get('tests', 0)); \
              failed = int(root.attrib.get('failures', 0)); \
              errors = int(root.attrib.get('errors', 0)); \
              skipped = int(root.attrib.get('skipped', 0)); \
              passed = total - failed - errors - skipped; \
              print('test_metrics=' + json.dumps({'passed': passed, 'failed': failed + errors, 'total': total, 'skipped': skipped}))" >> "$GITHUB_OUTPUT"
          else
            echo "test_metrics={\"passed\":0,\"failed\":0,\"total\":0,\"skipped\":0}" >> "$GITHUB_OUTPUT"
          fi

      - name: Thu thập Logs Backend
        id: capture-logs
        if: always()
        run: |
          LOGS_VAL=$(tail -n 100 pytest.log | base64 -w 0)
          echo "ci_logs=$LOGS_VAL" >> "$GITHUB_OUTPUT"

      - name: Tải artifact coverage backend lên
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: backend-coverage-report
          path: |
            backend/coverage.xml
            backend/htmlcov
          if-no-files-found: ignore

  docker-compose-smoke:
    runs-on: ubuntu-latest
    env:
      DOCKER_BUILDKIT: 0
      COMPOSE_DOCKER_CLI_BUILD: 0
    outputs:
      ci_logs: ${{ steps.capture-logs.outputs.ci_logs }}

    steps:
      - name: Tải mã nguồn
        uses: actions/checkout@v6

      - name: Chuẩn bị file môi trường Docker
        run: cp .env.docker.example .env

      - name: Build và khởi động docker compose
        run: docker compose -f docker-compose.ci.yml up -d --build --wait 2>&1 | tee docker_up.log

      - name: Chờ backend sẵn sàng
        run: |
          echo "### Health Check logs" > health.log
          for i in {1..15}; do
            if curl -fsS http://127.0.0.1:8000/health | tee -a health.log; then
              exit 0
            fi
            sleep 3
          done
          echo "Kiểm tra health của backend thất bại" | tee -a health.log
          docker compose -f docker-compose.ci.yml ps >> health.log
          docker compose -f docker-compose.ci.yml logs backend >> health.log
          exit 1

      - name: Kiểm tra frontend phản hồi
        run: curl -fsS http://127.0.0.1:3000 > /dev/null

      - name: Thu thập Logs Docker
        id: capture-logs
        if: always()
        run: |
          docker compose -f docker-compose.ci.yml logs --no-color > docker_full.log
          LOGS_VAL=$(tail -n 100 docker_full.log | base64 -w 0)
          echo "ci_logs=$LOGS_VAL" >> "$GITHUB_OUTPUT"

      - name: Dừng docker compose
        if: always()
        run: docker compose -f docker-compose.ci.yml down -v

  ci-summary:
    runs-on: ubuntu-latest
    needs: [frontend, backend, docker-compose-smoke]
    if: always()
    steps:
      - name: Tạo tóm tắt công việc chi tiết (Detailed Job Summary)
        uses: actions/github-script@v8
        with:
          script: |
            const frontendRes = "${{ needs.frontend.result }}";
            const backendRes = "${{ needs.backend.result }}";
            const dockerRes = "${{ needs.docker-compose-smoke.result }}";

            const frontendCov = JSON.parse('${{ needs.frontend.outputs.coverage_details }}' || '{"lines":0,"statements":0,"functions":0,"branches":0}');
            const frontendTests = JSON.parse('${{ needs.frontend.outputs.test_metrics }}' || '{"passed":0,"failed":0,"total":0,"skipped":0}');
            
            const backendCov = "${{ needs.backend.outputs.coverage }}" || "0";
            const backendTests = JSON.parse('${{ needs.backend.outputs.test_metrics }}' || '{"passed":0,"failed":0,"total":0,"skipped":0}');

            const decodeLogs = (b64) => {
              if (!b64) return "Không có log.";
              try {
                return Buffer.from(b64, 'base64').toString('utf-8');
              } catch (e) {
                return "Lỗi giải mã log.";
              }
            };

            const feLogs = decodeLogs("${{ needs.frontend.outputs.ci_logs }}");
            const beLogs = decodeLogs("${{ needs.backend.outputs.ci_logs }}");
            const dockerLogs = decodeLogs("${{ needs.docker-compose-smoke.outputs.ci_logs }}");

            const statusIcon = (res) => res === "success" ? "✅" : (res === "failure" ? "❌" : "⏭️");
            const statusText = (res) => res === "success" ? "Thành công" : (res === "failure" ? "Thất bại" : "Bỏ qua");

            const summary = `
            # 🚀 Báo cáo Tóm tắt CI Chi tiết (Dự Án AI Learning Studio)
            
            | Thông tin | Chi tiết |
            | :--- | :--- |
            | **Người thực hiện** | @${context.actor} |
            | **Nhánh** | \`${context.ref.replace('refs/heads/', '')}\` |
            | **Commit** | [\`${context.sha.substring(0, 7)}\`](${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/commit/${context.sha}) |
            | **Thời điểm** | ${new Date().toLocaleString('vi-VN', { timeZone: 'Asia/Ho_Chi_Minh' })} (HCMC) |
            
            ---

            ## 🖥️ Frontend (Vitest)
            **Trạng thái:** ${statusIcon(frontendRes)} ${statusText(frontendRes)}

            ### 📊 Thống kê Kiểm thử
            | Tổng số | ✅ Thành công | ❌ Thất bại | ⏭️ Bỏ qua |
            | :---: | :---: | :---: | :---: |
            | ${frontendTests.total} | ${frontendTests.passed} | ${frontendTests.failed} | ${frontendTests.skipped} |

            ### 📦 Độ phủ mã nguồn (Coverage)
            | Loại | Tỷ lệ | Tiến trình |
            | :--- | :---: | :--- |
            | **Lines** | \`${frontendCov.lines}%\` | ${'🟩'.repeat(Math.round(frontendCov.lines/10))}${'⬜'.repeat(10-Math.round(frontendCov.lines/10))} |
            | **Statements** | \`${frontendCov.statements}%\` | ${'🟩'.repeat(Math.round(frontendCov.statements/10))}${'⬜'.repeat(10-Math.round(frontendCov.statements/10))} |
            | **Functions** | \`${frontendCov.functions}%\` | ${'🟩'.repeat(Math.round(frontendCov.functions/10))}${'⬜'.repeat(10-Math.round(frontendCov.functions/10))} |
            | **Branches** | \`${frontendCov.branches}%\` | ${'🟩'.repeat(Math.round(frontendCov.branches/10))}${'⬜'.repeat(10-Math.round(frontendCov.branches/10))} |

            <details>
            <summary>🔍 Xem Logs Frontend</summary>

            \`\`\`
            ${feLogs}
            \`\`\`
            </details>

            ---

            ## ⚙️ Backend (Pytest)
            **Trạng thái:** ${statusIcon(backendRes)} ${statusText(backendRes)}

            ### 📊 Thống kê Kiểm thử
            | Tổng số | ✅ Thành công | ❌ Thất bại | ⏭️ Bỏ qua |
            | :---: | :---: | :---: | :---: |
            | ${backendTests.total} | ${backendTests.passed} | ${backendTests.failed} | ${backendTests.skipped} |

            ### 📦 Độ phủ mã nguồn (Coverage)
            | Loại | Tỷ lệ | Tiến trình |
            | :--- | :---: | :--- |
            | **Lines** | \`${backendCov}%\` | ${'🟩'.repeat(Math.round(backendCov/10))}${'⬜'.repeat(10-Math.round(backendCov/10))} |

            <details>
            <summary>🔍 Xem Logs Backend</summary>

            \`\`\`
            ${beLogs}
            \`\`\`
            </details>

            ---

            ## 🐳 Docker Smoke Test
            **Trạng thái:** ${statusIcon(dockerRes)} ${statusText(dockerRes)}

            <details>
            <summary>🔍 Xem Logs Docker</summary>

            \`\`\`
            ${dockerLogs}
            \`\`\`
            </details>
            
            ---
            *Ghi chú: Xem chi tiết logs tại tab Actions hoặc tải các Artifacts để xem báo cáo chi tiết.*
            `;
            
            core.summary.addRaw(summary).write();

  report-failure:
    runs-on: ubuntu-latest
    needs:
      - frontend
      - backend
      - docker-compose-smoke
    if: >-
      always() &&
      github.event_name == 'push' &&
      (needs.frontend.result == 'failure' || needs.backend.result == 'failure' || needs.docker-compose-smoke.result == 'failure')

    steps:
      - name: Tạo hoặc cập nhật GitHub Issue khi CI lỗi
        uses: actions/github-script@v8
        env:
          FRONTEND_RESULT: ${{ needs.frontend.result }}
          BACKEND_RESULT: ${{ needs.backend.result }}
          DOCKER_RESULT: ${{ needs.docker-compose-smoke.result }}
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const branch = context.ref.replace('refs/heads/', '');
            const title = `🚨 [CI Failure] Luồng CI thất bại trên nhánh ${branch}`;
            const runUrl = `${context.serverUrl}/${owner}/${repo}/actions/runs/${context.runId}`;
            
            const frontendStatus = process.env.FRONTEND_RESULT === 'success' ? '✅ Thành công' : '❌ Thất bại';
            const backendStatus = process.env.BACKEND_RESULT === 'success' ? '✅ Thành công' : '❌ Thất bại';
            const dockerStatus = process.env.DOCKER_RESULT === 'success' ? '✅ Thành công' : '❌ Thất bại';

            const body = [
              '## Báo cáo lỗi CI',
              '',
              'Hệ thống phát hiện lỗi trong quá trình kiểm tra tự động sau khi bạn đẩy code lên.',
              '',
              `- Người thực hiện: @${context.actor}`,
              `- Nhánh: \`${branch}\``,
              `- Commit: [\`${context.sha.substring(0, 7)}\`](${context.serverUrl}/${owner}/${repo}/commit/${context.sha})`,
              `- Workflow: \`${context.workflow}\``,
              '',
              '### Trạng thái các job',
              '| Job | Trạng thái |',
              '| :--- | :--- |',
              `| Frontend | ${frontendStatus} |`,
              `| Backend | ${backendStatus} |`,
              `| Docker Compose | ${dockerStatus} |`,
              '',
              '### Cách khắc phục',
              '1. Mở liên kết bên dưới để xem log chi tiết.',
              '2. Kiểm tra các bước lint, test hoặc build bị lỗi.',
              '3. Sửa lỗi ở máy local, chạy lại test và push lại.',
              '',
              `- Xem chi tiết tại GitHub Actions: ${runUrl}`,
              '',
              'Thông báo này được tạo tự động bởi GitHub Actions.',
            ].join('\n');

            const { data: issues } = await github.rest.issues.listForRepo({
              owner,
              repo,
              state: 'open',
              per_page: 100,
            });

            const existingIssue = issues.find((issue) => issue.title === title);

            if (existingIssue) {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: existingIssue.number,
                body: `🔄 **Cập nhật:** Pipeline lại tiếp tục thất bại tại commit ${context.sha.substring(0, 7)}.\n\n${body}`,
              });
            } else {
              await github.rest.issues.create({
                owner,
                repo,
                title,
                body,
                labels: ['bug', 'ci-failure', 'critical'],
                assignees: [context.actor]
              });
            }
